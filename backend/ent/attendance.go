// Code generated by ent, DO NOT EDIT.

package ent

import (
	"backend/ent/attendance"
	"backend/ent/event"
	"backend/ent/user"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Attendance is the model entity for the Attendance schema.
type Attendance struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CheckedIn holds the value of the "checked_in" field.
	CheckedIn bool `json:"checked_in,omitempty"`
	// RegistrationTime holds the value of the "registration_time" field.
	RegistrationTime time.Time `json:"registration_time,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AttendanceQuery when eager-loading is set.
	Edges             AttendanceEdges `json:"edges"`
	event_attendances *int
	user_attendances  *int
	selectValues      sql.SelectValues
}

// AttendanceEdges holds the relations/edges for other nodes in the graph.
type AttendanceEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Event holds the value of the event edge.
	Event *Event `json:"event,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AttendanceEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// EventOrErr returns the Event value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AttendanceEdges) EventOrErr() (*Event, error) {
	if e.Event != nil {
		return e.Event, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: event.Label}
	}
	return nil, &NotLoadedError{edge: "event"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Attendance) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case attendance.FieldCheckedIn:
			values[i] = new(sql.NullBool)
		case attendance.FieldID:
			values[i] = new(sql.NullInt64)
		case attendance.FieldRegistrationTime:
			values[i] = new(sql.NullTime)
		case attendance.ForeignKeys[0]: // event_attendances
			values[i] = new(sql.NullInt64)
		case attendance.ForeignKeys[1]: // user_attendances
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Attendance fields.
func (_m *Attendance) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case attendance.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case attendance.FieldCheckedIn:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field checked_in", values[i])
			} else if value.Valid {
				_m.CheckedIn = value.Bool
			}
		case attendance.FieldRegistrationTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field registration_time", values[i])
			} else if value.Valid {
				_m.RegistrationTime = value.Time
			}
		case attendance.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field event_attendances", value)
			} else if value.Valid {
				_m.event_attendances = new(int)
				*_m.event_attendances = int(value.Int64)
			}
		case attendance.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_attendances", value)
			} else if value.Valid {
				_m.user_attendances = new(int)
				*_m.user_attendances = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Attendance.
// This includes values selected through modifiers, order, etc.
func (_m *Attendance) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the Attendance entity.
func (_m *Attendance) QueryUser() *UserQuery {
	return NewAttendanceClient(_m.config).QueryUser(_m)
}

// QueryEvent queries the "event" edge of the Attendance entity.
func (_m *Attendance) QueryEvent() *EventQuery {
	return NewAttendanceClient(_m.config).QueryEvent(_m)
}

// Update returns a builder for updating this Attendance.
// Note that you need to call Attendance.Unwrap() before calling this method if this Attendance
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Attendance) Update() *AttendanceUpdateOne {
	return NewAttendanceClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Attendance entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Attendance) Unwrap() *Attendance {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Attendance is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Attendance) String() string {
	var builder strings.Builder
	builder.WriteString("Attendance(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("checked_in=")
	builder.WriteString(fmt.Sprintf("%v", _m.CheckedIn))
	builder.WriteString(", ")
	builder.WriteString("registration_time=")
	builder.WriteString(_m.RegistrationTime.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Attendances is a parsable slice of Attendance.
type Attendances []*Attendance
