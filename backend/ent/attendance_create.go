// Code generated by ent, DO NOT EDIT.

package ent

import (
	"backend/ent/attendance"
	"backend/ent/event"
	"backend/ent/user"
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// AttendanceCreate is the builder for creating a Attendance entity.
type AttendanceCreate struct {
	config
	mutation *AttendanceMutation
	hooks    []Hook
}

// SetCheckedIn sets the "checked_in" field.
func (_c *AttendanceCreate) SetCheckedIn(v bool) *AttendanceCreate {
	_c.mutation.SetCheckedIn(v)
	return _c
}

// SetNillableCheckedIn sets the "checked_in" field if the given value is not nil.
func (_c *AttendanceCreate) SetNillableCheckedIn(v *bool) *AttendanceCreate {
	if v != nil {
		_c.SetCheckedIn(*v)
	}
	return _c
}

// SetRegistrationTime sets the "registration_time" field.
func (_c *AttendanceCreate) SetRegistrationTime(v time.Time) *AttendanceCreate {
	_c.mutation.SetRegistrationTime(v)
	return _c
}

// SetNillableRegistrationTime sets the "registration_time" field if the given value is not nil.
func (_c *AttendanceCreate) SetNillableRegistrationTime(v *time.Time) *AttendanceCreate {
	if v != nil {
		_c.SetRegistrationTime(*v)
	}
	return _c
}

// SetUserID sets the "user" edge to the User entity by ID.
func (_c *AttendanceCreate) SetUserID(id int) *AttendanceCreate {
	_c.mutation.SetUserID(id)
	return _c
}

// SetUser sets the "user" edge to the User entity.
func (_c *AttendanceCreate) SetUser(v *User) *AttendanceCreate {
	return _c.SetUserID(v.ID)
}

// SetEventID sets the "event" edge to the Event entity by ID.
func (_c *AttendanceCreate) SetEventID(id int) *AttendanceCreate {
	_c.mutation.SetEventID(id)
	return _c
}

// SetEvent sets the "event" edge to the Event entity.
func (_c *AttendanceCreate) SetEvent(v *Event) *AttendanceCreate {
	return _c.SetEventID(v.ID)
}

// Mutation returns the AttendanceMutation object of the builder.
func (_c *AttendanceCreate) Mutation() *AttendanceMutation {
	return _c.mutation
}

// Save creates the Attendance in the database.
func (_c *AttendanceCreate) Save(ctx context.Context) (*Attendance, error) {
	_c.defaults()
	return withHooks(ctx, _c.sqlSave, _c.mutation, _c.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (_c *AttendanceCreate) SaveX(ctx context.Context) *Attendance {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *AttendanceCreate) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *AttendanceCreate) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (_c *AttendanceCreate) defaults() {
	if _, ok := _c.mutation.CheckedIn(); !ok {
		v := attendance.DefaultCheckedIn
		_c.mutation.SetCheckedIn(v)
	}
	if _, ok := _c.mutation.RegistrationTime(); !ok {
		v := attendance.DefaultRegistrationTime()
		_c.mutation.SetRegistrationTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (_c *AttendanceCreate) check() error {
	if _, ok := _c.mutation.CheckedIn(); !ok {
		return &ValidationError{Name: "checked_in", err: errors.New(`ent: missing required field "Attendance.checked_in"`)}
	}
	if _, ok := _c.mutation.RegistrationTime(); !ok {
		return &ValidationError{Name: "registration_time", err: errors.New(`ent: missing required field "Attendance.registration_time"`)}
	}
	if len(_c.mutation.UserIDs()) == 0 {
		return &ValidationError{Name: "user", err: errors.New(`ent: missing required edge "Attendance.user"`)}
	}
	if len(_c.mutation.EventIDs()) == 0 {
		return &ValidationError{Name: "event", err: errors.New(`ent: missing required edge "Attendance.event"`)}
	}
	return nil
}

func (_c *AttendanceCreate) sqlSave(ctx context.Context) (*Attendance, error) {
	if err := _c.check(); err != nil {
		return nil, err
	}
	_node, _spec := _c.createSpec()
	if err := sqlgraph.CreateNode(ctx, _c.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	_c.mutation.id = &_node.ID
	_c.mutation.done = true
	return _node, nil
}

func (_c *AttendanceCreate) createSpec() (*Attendance, *sqlgraph.CreateSpec) {
	var (
		_node = &Attendance{config: _c.config}
		_spec = sqlgraph.NewCreateSpec(attendance.Table, sqlgraph.NewFieldSpec(attendance.FieldID, field.TypeInt))
	)
	if value, ok := _c.mutation.CheckedIn(); ok {
		_spec.SetField(attendance.FieldCheckedIn, field.TypeBool, value)
		_node.CheckedIn = value
	}
	if value, ok := _c.mutation.RegistrationTime(); ok {
		_spec.SetField(attendance.FieldRegistrationTime, field.TypeTime, value)
		_node.RegistrationTime = value
	}
	if nodes := _c.mutation.UserIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   attendance.UserTable,
			Columns: []string{attendance.UserColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(user.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.user_attendances = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := _c.mutation.EventIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   attendance.EventTable,
			Columns: []string{attendance.EventColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(event.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.event_attendances = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// AttendanceCreateBulk is the builder for creating many Attendance entities in bulk.
type AttendanceCreateBulk struct {
	config
	err      error
	builders []*AttendanceCreate
}

// Save creates the Attendance entities in the database.
func (_c *AttendanceCreateBulk) Save(ctx context.Context) ([]*Attendance, error) {
	if _c.err != nil {
		return nil, _c.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(_c.builders))
	nodes := make([]*Attendance, len(_c.builders))
	mutators := make([]Mutator, len(_c.builders))
	for i := range _c.builders {
		func(i int, root context.Context) {
			builder := _c.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*AttendanceMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, _c.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, _c.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, _c.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (_c *AttendanceCreateBulk) SaveX(ctx context.Context) []*Attendance {
	v, err := _c.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (_c *AttendanceCreateBulk) Exec(ctx context.Context) error {
	_, err := _c.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (_c *AttendanceCreateBulk) ExecX(ctx context.Context) {
	if err := _c.Exec(ctx); err != nil {
		panic(err)
	}
}
